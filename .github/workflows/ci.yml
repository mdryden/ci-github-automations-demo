name: CI

on:
  push:
    branches:
      - main

  pull_request:

jobs:
  job1:
    name: Build and test 
    runs-on: ubuntu-latest

    steps:
      - name: Mock build
        run: echo "Building"

      - name: Mock test
        run: echo "Testing release"
